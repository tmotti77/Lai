"use client";
import type { InterviewSession, InterviewMessageRow } from "@/lib/interview/types";

export function InterviewChat(_: {
  session: InterviewSession;
  initialMessages: InterviewMessageRow[];
}) {
  return <div data-stub="InterviewChat" />;
}
